/** @namespace Storm::Component

The 'Component' namespace is a collection of algorithm control building blocks.

A Component is a data driven (from/to model points) entity that implements a 
function, algorithm, stuff, etc.  Components are designed to be run periodically.  

*/ 

  